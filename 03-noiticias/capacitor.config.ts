import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '03-noiticias',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
